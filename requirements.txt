# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.24.0

# Config & validation
pydantic-settings>=2.0

# HTTP client (Ollama calls + scraping)
requests>=2.31

# Vector store
chromadb>=0.5.0

# Embeddings & intent classification
sentence-transformers>=3.0
numpy>=1.24

# Scraping (only needed to rebuild the dataset)
beautifulsoup4>=4.12
lxml>=5.0
